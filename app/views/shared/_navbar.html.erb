<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container-fluid">
    <h2 class="ml14"><span class="text-wrapper"><span class="letters">La Galerie de Jerome</span><span class="line"></span></span></h2>
    <div class="collapse navbar-collapse" id="navbarNavDarkDropdown">
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Menu
          </a>
          <ul class="dropdown-menu dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <% if user_signed_in? %>
            <li><% if policy(Table).create? %><%= link_to "Add table", new_table_path, method: :get, class: "dropdown-item" %><% end %></li>
            <li><%= link_to "Update profile",edit_user_path(@user), method: :get, class: "dropdown-item" %></li>
            <li><%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
            <% else %>
            <li><%= link_to "Login", new_user_session_path, class: "dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
